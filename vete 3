let
    // Access the named range/array "XRPtree" from the current workbook
    Source = Excel.CurrentWorkbook(){[Name="XRPtree"]}[Content],
    
    // Keep the original structure but ensure proper types
    TypedData = Table.TransformColumnTypes(Source, {
        // Add specific column types as needed, for example:
        {"Tree_Name", type text},
        {"Value", type text},
        {"Parent", type text},
        {"V_S_C", type text}
        // Add other columns as needed
    })
in
    TypedData
